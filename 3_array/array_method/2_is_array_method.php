<?php
// is_array()
$arr = array(1, 2, 3);
if (is_array($arr)) {
    echo "is_array() method is working";
}
# Checks whether the variable is an array. Returns TRUE if the variable is an array,
# and FALSE otherwise.
